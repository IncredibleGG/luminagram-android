<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="auto">

    <application android:name="org.telegram.messenger.ApplicationLoaderImpl" tools:replace="name">
        <service
            android:name="org.telegram.messenger.GoogleVoiceClientService"
            android:exported="true"
            android:foregroundServiceType="dataSync"
        />
        <activity
            android:name="org.telegram.messenger.GoogleVoiceClientActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.voicesearch.SEND_MESSAGE_TO_CONTACTS" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <service
            android:name="org.telegram.messenger.SMSJobsNotification"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="dataSync|remoteMessaging"
        />

        <!-- LuminaGram: keeps the self-updater's APK download alive in the
             background and owns its progress notification. dataSync matches the
             two services already declared above; FOREGROUND_SERVICE and
             FOREGROUND_SERVICE_DATA_SYNC are declared in the library manifest. -->
        <service
            android:name="org.telegram.messenger.LuminaUpdateService"
            android:exported="false"
            android:foregroundServiceType="dataSync"
        />
    </application>
</manifest>